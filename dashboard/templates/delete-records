<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Record</title>
</head>
<body>
    <h1>Records</h1>
    <ul>
        {% for record in records %}
            <li>
                {{ record.name }} 
                <form action="{% url 'delete_titles' record.id %}" method="post" onsubmit="return confirm('Are you sure you want to delete this record?');">
                    {% csrf_token %}
                    <button type="submit">Delete Titles</button>
                </form>
                <form action="{% url 'delete_headings' record.id %}" method="post" onsubmit="return confirm('Are you sure you want to delete this record?');">
                    {% csrf_token %}
                    <button type="submit">Delete Headings</button>
                </form>
                <form action="{% url 'delete_activities' record.id %}" method="post" onsubmit="return confirm('Are you sure you want to delete this record?');">
                    {% csrf_token %}
                    <button type="submit">Delete Activities</button>
                </form>
            </li>
        {% endfor %}
    </ul>
</body>
</html>
